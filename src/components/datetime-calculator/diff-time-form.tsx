import React, { FC } from "react";

export interface DiffTimeFormProps { }

export const DiffTimeForm: FC<DiffTimeFormProps> = ({ }) => {
    return <>DiffTimeForm</>;
};

export default DiffTimeForm;
